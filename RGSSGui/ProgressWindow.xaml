<Window x:Class="RGSSGui.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Progress" Width="380" Height="130"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Loaded="ProgressWindow_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ProgressBar x:Name="progressBar" Grid.Row="0" Height="20" Minimum="0" Maximum="100" Margin="0,0,0,5"/>
        <TextBlock x:Name="pathLabel" Grid.Row="1" TextTrimming="CharacterEllipsis" Margin="0,0,0,5"/>
        <TextBlock x:Name="countLabel" Grid.Row="2" HorizontalAlignment="Center" Margin="0,0,0,5"/>
        <Button Grid.Row="3" Content="Cancel" Width="75" HorizontalAlignment="Center" Click="Cancel_Click"/>
    </Grid>
</Window>
